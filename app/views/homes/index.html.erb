<div class="cover">
  <div class="cover-inner">
    <div class="cover-contents">
      <h1>素敵なAPPと出会う場所</h1>
      <% unless user_signed_in? %>
        <%= link_to 'Job Chainを始める', new_user_session_path, class: 'btn btn-default btn-lg' %>
      <% end %>
      <% if user_signed_in? %>
        <div class="start-button">
          <%= link_to '登録へ', path, class: 'btn btn-default' %>
          <%= link_to 'APP一覧へ', apps_path, class: 'btn btn-default' %>
        </div>
      <% end %>
    </div>
  </div>
</div>
